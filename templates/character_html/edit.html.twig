{% extends 'base.html.twig' %}

{% block title %}Edition d'un personnage{% endblock %}

{% block body %}
    <div class="container-xl d-flex flex-column justify-content-center">
        <h1>Edition d'un personnage</h1>

        <div class="d-flex flex-column align-items-start">
            {{ include('character_html/_form.html.twig', {'button_label': 'Modifier'}) }}
            <a href="{{ path('character_html_index') }}">
                <button type="button" class="btn btn-primary">Retour à la liste</button>
            </a>
            {{ include('character_html/_delete_form.html.twig') }}
        </div>


    </div>
{% endblock %}
